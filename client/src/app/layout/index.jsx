import IntroPage from "../intro/page";
import HeaderNavbar from "./HeaderNavbar";

const RootLayout = () => {
  return (
    <section>
      <HeaderNavbar />
      <IntroPage />
    </section>
  );
};

export default RootLayout;
